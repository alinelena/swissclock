repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.2
    hooks:
      # Run the linter and automatically fix issues (including import sorting)
      - id: ruff
        args: [ --fix ]
      # Run the formatter
      - id: ruff-format
